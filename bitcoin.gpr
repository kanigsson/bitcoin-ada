with "gnatcoll";

project Bitcoin is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";

   package Compiler is
      for Switches ("ada") use ("-gnatwa", "-gnata", "-g");
   end Compiler;

   for Main use ("main2.adb", "main.adb");

end Bitcoin;

